# -------------------------------------------------------------------
# Client Frontend CI — push to dev / PR targeting main
# Only runs when medease-client/ files change
# -------------------------------------------------------------------
name: Client Frontend CI

on:
  push:
    branches: [dev]
    paths:
      - 'medease-client/**'
  pull_request:
    branches: [main]
    paths:
      - 'medease-client/**'

concurrency:
  group: client-frontend-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_ENV: production

defaults:
  run:
    working-directory: medease-client

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: medease-client/package-lock.json

      - name: Install dependencies
        run: npm install

      - name: ESLint validation
        run: npm run lint

      - name: Vite build
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: client-build
          path: medease-client/dist/
          retention-days: 3
